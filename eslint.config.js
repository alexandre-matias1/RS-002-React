import { defineConfig } from 'eslint-define-config';

export default defineConfig({
  extends: ["@rocketseat/eslint-config/react"],
  rules: {
    // Aqui você pode adicionar regras personalizadas, se necessário
  },
});